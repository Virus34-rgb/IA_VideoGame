# ============================================================
# Configuración del proyecto Castle Game
# ============================================================

# Parámetros de ejecución
VERSION: 1
RUN_NAME_SUFFIX: "evaluacion_scale"
N_BATCH: 2048
TRAIN_EPISODES: 500
EVAL_EPISODES: 100
SEED: null

# Flags del juego
USE_META_GAME: true
USE_WANDB: false
RUN_COMPARISON: true
DELETE_DIRECTORIES: true

# Flags de ejecución
RUN_SELF_PLAY: true
RUN_EVALUATION: true
HUMAN_OPPONENT: "none"
HUMAN_EPISODES: 20
HUMAN_EPSILON: 0.1
PLAY_AGAINST_AI: false
PLAY_EPISODES: 20
PLAY_EPSILON: 0.0

# ============================================================
# Comparaciones de runs (solo se usan si RUN_COMPARISON = true)
# ============================================================
comparisons:
  - run_name: "no_scale"
    N: 2048
    train_batches: 150
    eval_batches: 30
    seed: 42
    overrides:
      REWARD_SCALE: 1.0

  - run_name: "scale100"
    N: 2048
    train_batches: 150
    eval_batches: 30
    seed: 42
    overrides:
      REWARD_SCALE: 100.0